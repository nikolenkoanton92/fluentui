import { FluentProvider, webLightTheme, Button, Link, Badge } from '@fluentui/react-components';

console.log(FluentProvider, webLightTheme, Badge, Button, Link);

export default {
  name: 'Scenario (Provider + theme + Badge + Button + Link)',
  threshold: null,
};
